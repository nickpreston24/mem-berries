{
    "name": "memberries",
    "version": "0.1.0",
    "private": true,
    "homepage": "https://mikepreston17.github.io/mem-berries/",
    "dependencies": {
        "gh-pages": "^2.0.1",
        "react": "^16.8.6",
        "react-dom": "^16.8.6",
        "react-scripts": "3.0.1"
    },
    "scripts": {
        "predeploy": "yarn run build",
        "deploy": "gh-pages -b master -d build",
        "start": "react-scripts start",
        "build": "react-scripts build",
        "test": "react-scripts test",
        "eject": "react-scripts eject",
        "merge-deploy": "git checkout master && git merge source && git checkout source && yarn deploy"
    },
    "eslintConfig": {
        "extends": "react-app"
    },
    "browserslist": [
        ">0.2%",
        "not dead",
        "not ie <= 11",
        "not op_mini all"
    ]
}